# Use a flat, npm-style node_modules (real folders, no symlinks).
# The default symlinked layout breaks inside the standalone release on the
# way through git to the server — Next then can't find packages like styled-jsx.
node-linker=hoisted
